//Importamos todas las clases que necesita la Vista (Arquitectura VIP).
const evaConnectView = require('../Views/evaConnectView');
const managerView = require('../Views/managerView');
const responseView = require('../Views/responseView');

//Exportamos todas las clases que necesita la Vista (Arquitectura VIP).
module.exports.evaConnectView = evaConnectView;
module.exports.managerView = managerView;
module.exports.responseView = responseView;