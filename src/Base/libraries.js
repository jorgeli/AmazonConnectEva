//Importamos todas las librerías utilizadas por el proyecto.
const utils = require('../Utils/utils');
const axios = require('axios');
const qs = require('qs');

//Exportamos todas las librerías utilizadas por el proyecto.
module.exports.utils = utils;
module.exports.axios = axios;
module.exports.qs = qs;

